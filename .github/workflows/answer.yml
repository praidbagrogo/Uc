name: blank

on:
  push:

jobs:
  insert:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        collection: ["migration", "appreciate", "invade", "delightful", "score"]
        poison: ["immobile", "yawn", "recent", "discover"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar co
